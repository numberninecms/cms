<div class="flex-grow p-3">
    {{ form_start(form, {attr: {'data-turbo': 'false'}}) }}
        {% include '@NumberNine/admin/util/content_entities_table.html.twig' with {
            entities: deleted_media_files,
            form: form,
        } %}

        <div class="flex">
            {{ form_row(form.restore) }}
            {{ form_row(form.delete, {row_attr: {class: 'btn btn-color-red'}}) }}
        </div>
    {{ form_end(form) }}
</div>
